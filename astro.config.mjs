// @ts-check
import { defineConfig } from 'astro/config';

// Quando tiver um domínio, troque a URL abaixo (ex.: https://viniciusmiguel.com.br)
export default defineConfig({
  site: 'https://viniciusmiguel.com.br',
});
